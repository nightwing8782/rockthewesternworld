DirectoryIndex index.html
Options -Indexes +FollowSymLinks

RewriteEngine On
RewriteBase /

# Route Studio directly to journal.html
RewriteRule ^journal/?$ journal.html [L]

# Clean HTML URLs (e.g. /slug -> /slug.html)
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME}.html -f
RewriteRule ^(.*)$ $1.html [L]

# Smart 404 Resolver for newly published Supabase articles
ErrorDocument 404 /_not-found.html
